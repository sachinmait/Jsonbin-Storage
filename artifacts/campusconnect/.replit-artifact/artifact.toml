kind = "web"
previewPath = "/"
title = "CampusConnect"
version = "1.0.0"
id = "artifacts/campusconnect"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20795

[services.development]
run = "pnpm --filter @workspace/campusconnect run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/campusconnect", "run", "build" ]
publicDir = "artifacts/campusconnect/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20795"
BASE_PATH = "/"
